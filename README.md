# untuk install pake perintah ini #
```
apt update && apt upgrade -y && apt install build-essential -y && apt-get install -y jq && apt-get install shc && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/sajauguida7678982eguhguw78t376r6736734f/djgihiudygjdopglpdklpgidgisuougodg/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
